server:
  port:
    8084
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lambda_track?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: "*Mareen7#"
    driverClassName: com.mysql.cj.jdbc.Driver
  application:
    name: tracking-service
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update